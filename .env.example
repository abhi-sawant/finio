# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# The base URL of your deployed PHP backend (no trailing slash).
EXPO_PUBLIC_API_URL=https://api.yourdomain.com
